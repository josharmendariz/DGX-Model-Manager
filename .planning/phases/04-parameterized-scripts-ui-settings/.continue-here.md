# Resume here — Phase 4, wave 3 (paused 2026-08-20 evening, user called it a night)

Branch: `rec-approve-actions` (PR #4). Tree clean except an unrelated `.planning/STATE.md`
edit owned by a concurrent session — do not commit it.

## Done
- **Wave 1 / 04-01** — parameterized generated scripts (`${VLLM_MAX_MODEL_LEN:-…}` etc.),
  `_OVERRIDE_ENV` allow-list + `_resolve_overrides()` (400 on unknown/out-of-range),
  overrides through `Popen(env=)`, resolution moved ahead of `_vram_admission_check`,
  `exec docker run` → `docker run -d`. Commits `775c1fd 4c4a14e 8639fe0 41638ec`.
- **Wave 2 / 04-02** — derived/recommended/warnings persisted as versioned header comments,
  read back by `_parse_script_meta`, profile-card launch settings UI.
  Commits `b5507bf e175043 793cbcc 863276c`.
- Suite: 337 → **404 passing**.

## Not done
- **Wave 3 / 04-03** (`autonomous: false`) — legacy-flag regex parser + 4-state classifier,
  parameterize diff→apply with sha256 confirm-token, `esc()` escaping pass.
  Not started. Josh was asked how to run it and deferred; **ask again before dispatching.**
  Options offered: executor-with-no-live-writes (recommended), executor-with-checkpoints,
  interactive inline, or skip to verification.
- **04-02 Task 3 human-verify steps** — restart the service, launch with util 0.60,
  `docker inspect vllm_node` to confirm the override actually reaches the container.
  Everything but the browser→HTTP hop is unit-tested. Phase verification must run this
  before signing off success criterion 3.
- Phase verification (`gsd-verifier`) — deferred until wave 3 resolves.

## Load-bearing carry-overs for 04-03
- `renderProfileSettings(p)` branch order matters: `meta_error` → `derived.editable === false`
  → editable → `if (!d) return ''`. **That final empty return is 04-03's insertion point**
  for read-only/unparseable. `test_meta_error_is_branched_on_before_classification` fails if
  the `meta_error` branch moves below it.
- Warning/error text is currently set via `textContent` after `innerHTML`, keyed by index over
  `.profile-item`; `test_warning_and_error_text_are_written_with_textcontent` asserts
  `p.warnings` never appears inside `renderProfileSettings`. When `esc()` lands, either keep
  that or change the test deliberately.
- Header format is versioned (`_SCRIPT_META_VERSION`, `"v": 1`). A parameterize rewrite of a
  legacy script must emit all four headers via `_script_meta_headers`, never hand-built.
- `_derive_launch_spec` **clamps** an over-requested context (warns and discards, does not
  apply). Surfaced in the UI by 04-02; keep it surfaced.
- `_launch_defaults(resolved)` is the only source of the three placeholder defaults.
- The live Qwen3.6 recipe wrapper serves production traffic — any real apply against
  `profiles/vLLM/` is a separate diff-then-adopt decision, not an executor's to make.

## Repo hygiene
~6 concurrent Claude sessions run here. Commit by explicit path, never `git add -A`.
