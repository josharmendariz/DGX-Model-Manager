# Resume here — Phase 3 planning (paused 2026-08-05, context exhaustion)

Branch: `rec-approve-actions`

## Where we stopped
`/gsd-plan-phase 3` ran through research and stopped BEFORE the planner.

Done and committed:
- `.planning/phases/03-curated-recipe-overrides/03-CONTEXT.md` (260923f)
- `.planning/phases/03-curated-recipe-overrides/03-CODEX-SURVEY.md` (260923f)
- `.planning/phases/03-curated-recipe-overrides/03-RESEARCH.md` (87843f9)

Not done: VALIDATION.md, gsd-pattern-mapper, gsd-planner, gsd-plan-checker, PLAN.md files.

## First action on resume
Ask Josh the two open decisions (he was mid-question when we ran out of context):

1. **Template-inferred parsers** — do capability-map entries that are only *inferred*
   (Qwen3-8B/14B/1.7B, Qwen2.5-14B, Qwen3-Next, Qwen3-VL → `hermes`) emit their parser,
   or stay silent? Emitting gives working tool calls but risks silent corruption; staying
   silent strictly removes the wrong `qwen3_coder` and adds no risk. Researcher recommends
   conservative-emit with a per-entry `emit` field. This is researcher OQ-1.
2. **gpt-oss KV** — confirm the opt-in-allowlist resolution. Its recipe sets fp8 KV but its
   hand-corrected profile uses full-precision and roadmap SC3 requires that, so
   "recipe always wins" would regress SC3. Claude's call: only explicitly-mapped models are
   recipe-backed, gpt-oss stays unmapped. NOT yet confirmed by Josh.

Also resolve researcher OQ-4: `02-MODEL-FIXTURES.json` has no `architectures` column that
the capability map needs — regenerate it in Wave 0 rather than adding a second fixture source.

Then: create VALIDATION.md from RESEARCH.md's `## Validation Architecture`, spawn
`gsd-pattern-mapper`, then `gsd-planner` (2 plans per ROADMAP), then `gsd-plan-checker`.

## After PLAN.md exists
Josh asked for a cross-AI plan review via `/gsd-review` (Codex second opinion), then converge.

## Repo hygiene
~3 concurrent Claude sessions run here. Commit by explicit path, never `git add -A`.
Uncommitted and NOT ours: `profiles/vLLM/start_hf_openai_gpt-oss-120b.sh` (peer session),
`start_hf_qwen_qwen3-8b.sh`, `start_hf_qwen_qwen3-14b.sh` (pre-existing untracked).
