# Runtime config (contains user-specific URLs and hashed API key)
config.json

# Runtime data written by the app
custom_dirs.json
hf_meta_cache.json

# Local working dirs
backup/

# Python
venv/
__pycache__/
*.pyc

# OS
.DS_Store
Thumbs.db
