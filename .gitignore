# Runtime config (contains user-specific URLs and hashed API key)
config.json

# Runtime data written by the app
custom_dirs.json
hf_meta_cache.json
alert_state.json
agent_runs.json

# Research-refresh working + review artifacts (recommendations.json IS tracked)
recommendations.proposed.json
.research_prompt.txt
.research_schema.json
.research_out.json
.research_run.sh

# Local working dirs
backup/

# Python
venv/
.venv/
__pycache__/
*.pyc

# OS
.DS_Store
Thumbs.db
misc/

# Image drift report written by image_updates.py
image_drift.json

# Timestamped local backups and session checkpoints (gb10 git-reconcile
# cleanup, 2026-09-21): not source, safe to keep on disk, noisy in git status.
*.bak-*
.last-session.json
